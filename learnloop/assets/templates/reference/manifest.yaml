name: reference
version: 1
renderer: semantic-html-v1
supports:
  blocks: [paragraph, list, code, callout, heading, section, exercise, checkpoint, table, concept, compare, evidence, figure, gallery, flow, timeline, decision]
requires:
  section_ids: true
  question_buttons: true
assets:
  css: style.css
  js: runtime.js
