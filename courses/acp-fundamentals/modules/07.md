---
id: m7
title: "把 ACP 桥接到浏览器"
summary: "浏览器无法启动本地子进程，因此需要一个本地 bridge 来持有 ACP 进程。"
template: case
---

## [m7-why] 浏览器为什么需要桥接

浏览器页面不能安全地启动本地 Agent 进程，也无法直接管理 stdin/stdout 管道。

常见做法是引入一个**本地 bridge**：浏览器与 bridge 通过 HTTP 或 SSE 通信；bridge 再通过 stdio 与 ACP Agent 通信。

::: checkpoint
为什么浏览器不能直接管理 stdio？这涉及浏览器的哪个核心安全限制？

--- answer
浏览器的沙箱不允许网页直接访问本地文件系统或启动子进程；这是同源策略与沙箱安全模型的基本约束。
---
:::

## [m7-architecture] Bridge 架构

```text
Browser UI
  | HTTP 或 SSE
Local Bridge
  | JSON lines over stdio
ACP Agent
```

bridge 负责：进程生命周期、请求 id、工具策略、流转换。

## [m7-http] 最小 HTTP 职责

一个小型 bridge 最初只需要承担这些职责：

- 接收来自浏览器的用户消息。
- 把它转换成 ACP 请求。
- 读取 Agent 响应。
- 以流式或一次性方式返回答案。
- 把日志和错误存到可调试的位置。

## [m7-sse] HTTP、SSE 还是 WebSocket

- **HTTP**：适合请求-响应式交互。
- **SSE**：适合把 Agent 的流式文本推送到浏览器。
- **WebSocket**：适合双方需要频繁双向通信的场景。

从最简单的传输开始，只要能保留学习闭环即可。

::: checkpoint
如果只需要“用户发一条消息，Agent 流式返回一段文字”，应该优先选 HTTP、SSE 还是 WebSocket？

--- answer
SSE，因为浏览器只需要接收服务器推送的流式文本，不需要频繁双向通信。
---
:::

## [m7-summary] 模块小结

- 浏览器需要 bridge 才能使用本地 ACP Agent。
- bridge 负责进程和协议的整洁性。
- SSE 通常足以支撑流式回答。
- 在本地闭环被证明有用之前，不要急着搭建完整平台。

::: exercise
你正在设计一个浏览器内的 ACP 助手。团队提出了两种方案，请选择更合理的一种。

- A. 浏览器页面直接通过 JavaScript 的 `child_process` 启动本地 Agent。
- B. 浏览器通过 SSE 连接一个本地 bridge；bridge 启动并持有 stdio Agent 进程。
- C. 浏览器通过普通 HTTP 轮询 Agent 进程的标准输出。
- D. 浏览器直接使用 WebSocket 与 Agent 进程通信。

--- answer
**B** 最合理。浏览器无法直接启动子进程，bridge 提供了必要的进程隔离和协议转换；SSE 适合流式回答。A 违反浏览器安全模型；C 的轮询效率低；D 在没有双向高频通信需求时比 SSE 更复杂。
---
:::
