interface:
  display_name: "LearnLoop"
  short_description: "Generate and maintain verified local learning courses"
  default_prompt: "Use $learnloop to generate or improve a course with source inventory, claims, validation, and build output."
