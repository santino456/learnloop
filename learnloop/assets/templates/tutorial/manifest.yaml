name: tutorial
version: 1
renderer: semantic-html-v1
supports:
  blocks: [paragraph, list, code, callout, heading, section, checkpoint, table]
requires:
  section_ids: true
  question_buttons: true
assets:
  css: style.css
  js: runtime.js
